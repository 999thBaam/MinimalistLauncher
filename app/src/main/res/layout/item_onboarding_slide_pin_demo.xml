<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Pin Demo onboarding slide.
    Shows the Maglev Shift animation demonstrating pinning.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:background="@color/background">

    <!-- Feature Label -->
    <TextView
        android:id="@+id/featureLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FEATURE"
        android:textSize="11sp"
        android:letterSpacing="0.15"
        android:fontFamily="sans-serif-medium"
        android:textColor="#666666"
        android:layout_marginBottom="12dp"
        android:alpha="0" />

    <!-- Title -->
    <TextView
        android:id="@+id/slideTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pin what matters"
        android:textSize="28sp"
        android:letterSpacing="-0.02"
        android:fontFamily="sans-serif-medium"
        android:textColor="@color/minimalist_text_primary"
        android:layout_marginBottom="24dp"
        android:alpha="0" />

    <!-- Real App List for Pinning -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/pinDemoRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:overScrollMode="never" />

    <!-- Minimal instruction -->
    <TextView
        android:id="@+id/slideDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Tap to pin your favorites"
        android:textSize="16sp"
        android:textColor="@color/minimalist_text_secondary"
        android:textAlignment="center"
        android:alpha="0" />

</LinearLayout>
