<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Android 11+ to query installed apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    
    <!-- Firebase requires internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Lock screen feature -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    
    <!-- Intent Firewall: VIP Detection (optional, graceful fallback) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinimalistLauncher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true">
            
            <!-- Standard launcher entry -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Register as a Home screen replacement -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Phone Auth Activity - appears before onboarding -->
        <activity android:name=".PhoneAuthActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MinimalistLauncher"/>
        
        <activity android:name=".OnboardingActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MinimalistLauncher"/>
        
        <!-- Gatekeeper Activity - Progressive disclosure task tracker -->
        <activity android:name=".GatekeeperActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MinimalistLauncher"/>
        
        <!-- Lock Screen Activity - Shows over system lock screen -->
        <activity 
            android:name=".LockScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MinimalistLauncher"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true" />
        
        <!-- Shadow Inbox - Ghost Mode message reader -->
        <activity 
            android:name=".ShadowInboxActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MinimalistLauncher" />
            
        <service android:name=".service.SmartNotificationListener"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        
        <!-- Gatekeeper Widget -->
        <receiver
            android:name=".widget.GatekeeperWidgetProvider"
            android:exported="true"
            android:label="Gatekeeper">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.minimalist.launcher.TOGGLE_TASK" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gatekeeper_widget_info" />
        </receiver>
        
        <!-- Screen On/Off Receiver for custom lock screen -->
        <receiver
            android:name=".service.ScreenReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".IntentFirewallSettingsActivity"
            android:label="Intent Firewall Settings"
            android:theme="@style/Theme.MinimalistLauncher"
            android:exported="false" />
            
    </application>

</manifest>

